报告已生成并存档：memory_quality_report_20260828.md。完整内容如下。

# 记忆质量测试报告（2026-08-28）

## 一、测试概况

模拟 3 组多对象对话（杭州赴约阿明 / 陈姐评审被否 / 西湖 demo 计划）→ 真实 LLM（deepseek-v4-flash）边界检测 + 事件充实 → 审计落位 → 召回评估 → 三轮递归改进。数据库累积不清库，嵌入用 hash（与索引一致），评估工具默认只读（不写记忆强度）。

## 二、记忆落位情况（10 个可见事件）

- EVT-6f32e5f4（08-24）：为记忆存储测试新开 codex 分支，deepseek 已切换。对象 deepseek，trust .5，act/ff 0.60/1.149。
- EVT-ffe13023（08-24）：新建子目录、数据库与过程日志落库、以对话为输入。对象 deepseek，trust .7，0.60/1.149。
- EVT-1cf966b1（08-24）：提醒测试规则：每轮回答+持续测试；追加 331 字未达阈值。对象 deepseek，anger .2，0.30/0.758。
- EVT-e8f99c25（08-24）：四项修复落地：批内合并/换行分隔/去重/情感示例。对象 deepseek，joy .6，0.70/1.320。
- EVT-00f33beb（08-24）：真实验证：边界 6→1 次、LLM 11→4 次、事件 5→3 个。对象 deepseek，trust .6，0.40/0.871。
- EVT-bc247b06（08-24）：最近 3 轮不清库；修复 mark_ingested 丢轮 bug。对象 deepseek，trust .8，0.60/1.149。
- EVT-a5a67d6d（08-24）：保留 white_painting_entries 分级白描方案。对象 deepseek，trust .8，0.60/1.149。
- EVT-60fc0a29（08-27）：推荐实现方式 + 事件/对象/遗忘因子与回忆关系 + P0-P3。对象 deepseek，antic .7，0.70/1.320。
- EVT-22e723ec（08-27）：阿明约下周四杭州聚；查车次天气：中雨、7:05、带伞。对象 deepseek、阿明，joy .5，0.70/1.320。
- EVT-3ba2c3c0（08-28）：评审会被否失落；deepseek 劝补数据；带 demo 给阿明。对象 deepseek、陈姐、阿明，sadness .6，0.60/1.149。

白描：新事件三级白描完整（l1 提及 → l2 互动 → l3 意图，原文可支撑、对象无情感无等级）；旧事件只有 role_summary（分级上线前历史数据）。已知瑕疵：EVT-22e723 残留修复前的幽灵对象陈姐白描（代码已修，未回溯清理）。未闭环：rounds_3/rounds_4（113/198 字）被边界判定未闭环，内容保留在 shadow/UC 库，未丢失。

## 三、回忆内容与质量（13 条标注查询）

Recall@1 = 0.538 / Recall@3 = 0.769 / Recall@5 = 0.846 / MRR = 0.673。

命中要点：整批合并格式分隔续写去重、事件对象遗忘因子和回忆、跑一下测试最近几轮不清库 → R@1 全中；模拟事件：陈姐方案否定心情、杭州阿明高铁天气 → R@1 命中（对象路生效）；西湖阿明 demo → R@3（阿明为主角的杭州事件排第一，属正常竞争）；两例 miss：两件独立的事、边界调用次数 → 查询措辞与摘要措辞词汇桥缺失（已加 L1 可检索性提示词）。

消融（污染清除前）：只词法 = 全通道，只语义略低，+近因无变化——小库下词法主导，语义/近因无增量。

## 四、三轮迭代：发现 → 修复 → 数据

1. R1 发现：对象串扰（整批对象并集传给每个事件 → 幽灵白描）；测量污染（reinforce=on 把事件 ff 堆到 4~114 倍，压垮召回）。
2. R2 修复：对象按内容归属（SubSegment 段级记录）；ff 重置 7 行；测量默认只读；强化 1.5x/300 → 1.15x/3.0。Recall@1 0.10→0.50，MRR 0.295→0.625。
3. R3：标注集扩到 13 条；L1 摘要要求保留原文关键短语/数字。R@1=0.538 / R@5=0.846，提示词效果待真实对话闭环验证。

## 五、结论与遗留

结论：记忆落位质量合格（摘要/白描/情绪无幻觉、与内容吻合）；回忆质量受数据污染影响远大于算法——清除污染后 Recall@1 提升 5 倍，证明测量必须只读；对象路对多对象事件有效。

遗留：旧白描 l1/l2/l3 为空（不回溯）、幽灵对象白描（可清理）、低唤醒规则记忆排序吃亏（arousal 当强度的设计约束）、L1 提示词待复测、qdrant 本地模式维度切换缓存 bug（已记录）。

本轮对话照惯例下轮追加进 conversation.jsonl。当前改动未提交，需要提交说一声。

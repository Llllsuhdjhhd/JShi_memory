name: jshi-memory-test
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - pytest>=7.4
      - pytest-asyncio>=0.21
      - openai>=1.0
      - pydantic>=2.0
      - pydantic-settings>=2.0
      - sqlalchemy>=2.0
      - qdrant-client>=1.7
      - opencc-python-reimplemented>=0.1.7
      - fastapi>=0.100
      - uvicorn>=0.20

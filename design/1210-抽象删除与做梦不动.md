# 1210 抽象删除 · 做梦不动

> 范围：1210–1409（子文档可用 1220–1400）｜状态：已确认（2026-08-23）

## 1. 范围

- **删除抽象链**：`AbstractionService`、`AbstractionShadowingService`（ASF）、`InductiveEvolutionSkill`、
  `mining/`（频繁子集挖掘）、`abstracted_subsets`、`recall_log`（抽象触发路径专用，非回忆）、
  Event 抽象字段（`is_abstract` / `abstraction_level` / `source_events` / `abstract_coverage` 等，字段级清理随 1010）；
- **做梦不动**：`DreamEvolutionSkill`、`jobs/dream_consolidation`、`origin=dream` 保留；
- **回忆按 1010 重写**：旧回忆模块（RecallService、tri_band、retrieval backends、BM25、Tier-1 等）**不保留**，
  完全按 [1010-回忆设计.md](1010-回忆设计.md) 新方案替换；回忆恢复（遗忘因子强化）保留。

> **注意**：此处删除的 `recall_log` 是"抽象触发路径专用"的频繁子集登记表。**不要与本轮新增的
> `recall_traces`（[1010 §8.3](1010-回忆设计.md#83-说话人归属interlocutor与回忆轨迹recall_traces＋软纠偏)）混淆**——
> `recall_traces` 是每次 recall 的**观测轨迹**（输入 + 命中条目含 `interlocutor`），只做回溯，**不触发抽象**。

## 2. 执行状态

- [x] 删除抽象服务 / 技能 / 挖掘模块与仓储、表（1410 清单）；
- [x] `REMSPipeline` 装配与 `ingest` 中移除抽象与 recall_log 调用；
- [ ] Event 抽象字段清理（随 1010，避免破坏回忆代码）；
- [ ] 抽象配置项清理（随 1410 收尾）。

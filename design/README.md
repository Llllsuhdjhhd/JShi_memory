# 匠石记忆后端（Jshi_memory）设计文档库

本目录是本仓库（REMS3 fork）改造的设计文档库：**先写清每一部分要改什么、为什么改、目标是什么、与哪些模块相关、预留什么、有哪些待讨论点，确认后再动代码。**

## 编号规则

- 每部分从 10 开始，间隔 200：`10, 210, 410, 610, 810, ...`
- 编号为 N 的部分，其全部内容（含子文档）写在 `[N, N+200)` 区间内
- 子文档使用区间内的中间编号，例如 10 部分的子文档可用 `20, 30, ..., 200`
- 每部分一个主文档，文件名格式：`<编号>-<名称>.md`

### `refactor/` 子目录（重构文档，独立编号）

`design/refactor/` 存放重构过程中的辅助文档（流程图、数据格式样例、迁移清单等），**不参与上面的章节编号**，
自带一套编号：

- 前缀固定 `cg-`（cg = 重构 / chong gou）；
- **每个子类按 100 起跳**：`cg-100`、`cg-200`、`cg-300` …（100 = 总体流程，200 = 数据格式，后续子类顺延）；
- **同一子类内按 1 或 2 递增**：`cg-101`、`cg-102` / `cg-201`、`cg-202` …（每篇新文档 +1，成对文档用 +2）；
- 文件名格式：`cg-<子类号+序号>-<名称>.md`。

## 章节索引

| 编号 | 区间 | 文档 | 状态 |
|------|------|------|------|
| 10 | 10–209 | 总体设计与主体模型 | 已确认 |
| 210 | 210–409 | 输入协议 | 已确认 |
| 220 | （210 子文档） | 投递策略（30 侧批次冲刷） | 草稿 |
| 410 | 410–609 | 对象时间线（角色系统改造） | 已确认 |
| 610 | 610–809 | 事件封装 | 已确认 |
| 810 | 810–1009 | 存储与 Schema | 已确认 |
| 1010 | 1010–1209 | 回忆设计（RAG 选型与检索管线） | 已确认 |
| 1210 | 1210–1409 | 抽象删除 · 做梦不动 | 已确认 |
| 1410 | 1410–1609 | 清理与删除清单 | 已确认 |
| 1610 | 1610–1809 | 测试与验收 | 待写 |

状态说明：`草稿` = 内容待评审；`已确认` = 评审通过；`已实施` = 代码完成且测试通过。

## `refactor/` 目录索引

| 编号 | 子类 | 文档 | 状态 |
|------|------|------|------|
| cg-100 | 100 总体流程 | [主要流程图](refactor/cg-100-主要流程图.md)（写入 / 代谢 / 封装 / 回忆 / 肖像 / 遗忘） | 草稿 |
| cg-200 | 200 数据格式 | [数据格式样例](refactor/cg-200-数据格式样例.md)（一段对话的端口格式与落库字段） | 草稿 |
| cg-201 | 200 数据格式 | [提示词对照草稿](refactor/cg-201-prompt-compare-draft.md)（原 REMS3 vs 本仓：边界 / 充实） | 草稿 |
| cg-300 | 300 更改方案 | [记忆模型收口](refactor/cg-300-事件形成收口.md)（记忆侧按概念文档收口；认知侧留在匠石，只保留写回位置） | 草稿 |

## 每部分文档模板

每部分主文档必须包含以下小节（未定项写"待讨论"）：

1. **改动内容** —— 具体改哪些代码 / 数据 / 接口
2. **原因** —— 为什么必须改（对应哪条需求约束）
3. **目标** —— 改完后的可验证结果
4. **与其他模块的关系** —— 上下游依赖、影响面
5. **预留的功能** —— 本次不做但需保留的接口 / 数据结构 / 扩展点
6. **待讨论问题** —— 需要拍板的点
7. **验收标准** —— 跑哪些测试、达到什么结果

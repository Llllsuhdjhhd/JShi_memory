# 1410 清理与删除清单

> 范围：1410–1609｜状态：已确认（2026-08-23）

## 1. 已删除（本轮）

### 模块

- `src/rems/services/abstraction_service.py`
- `src/rems/services/abstraction_shadowing.py`（ASF）
- `src/rems/services/abstraction_constants.py`
- `src/rems/skills/inductive_evolution.py`
- `src/rems/mining/`（closed_itemsets 频繁子集挖掘）

### 存储

- `database.py`：删除 `RecallLogRecord`、`AbstractedSubsetRecord` 表；
- `repository.py`：删除 `RecallLogRepository`、`AbstractedSubsetRepository` 与 `_subset_fingerprint`；
- `pipeline.py`：删除抽象与 recall_log 装配与调用；`run_evolution` 移除；`api/routes.py` `/evolve` 端点移除。

### 测试

- `tests/test_abstraction_service.py`
- `tests/unit/test_abstract_coverage.py`
- `tests/unit/test_asf_propagation.py`
- `tests/unit/test_inductive_evolution.py`
- `tests/unit/test_narrative_dup.py`
- `tests/unit/test_recall_pipeline_recall_log.py`
- `tests/unit/test_pipeline.py`：移除 `abstraction_service` 断言

### 回忆模块（1010 重写后删除）

- `services/recall_service.py`、`recall_quality.py`、`recall_bypass.py`；
- `retrieval/`（backends / bm25 / channels / factory / protocol / rrf / types）；
- `embedding/`（tri_band / act_text / query_act_processor）；
- `storage/vector_store.py`、`qdrant_store.py`、`tier1_store.py`、`active_pool_cache.py`；
- `skills/recall_query_compress.py`、`recall_intent.py`、`recall_block_relevance.py`；
- `ingest/focus_roles.py`、`strategies/recall.py`、`llm/ingest_session.py`、`jobs/tier1_refresh.py`；
- 旧回忆测试（test_recall_*、test_tri_band_encoder、test_act_text、test_query_act_processor、
  test_focus_roles、test_ingest_llm_session、test_tier1_ares、test_dynamic_recall_v2、test_strategy_interfaces）
  删除，替换为 `tests/unit/test_recall_pipeline.py`。

## 2. 待后续（不在本轮）

- `RoleExtractionSkill` 与 UserMode / `core_user_role_id` / `active_participants`（410 范围，随旧 ingest 收尾）；
- `ProcessingMode`（DIALOGUE / PASSIVE_LOG / NPC_AGENT）输出模式（210 定稿决策 9）；
- Event 抽象字段与抽象配置项（随 1010 / 收尾）；
- 场景脚本（`tests/scenarios/hongloumeng/runner.py`、`tests/integration/...`）中的抽象引用（非单元基线，另行处理）。
